"""Single source of truth for project-name-derived constants (console).

If the project is renamed, edit this file (and the matching one in the
parent `enrich` package). Code and prose reference these constants
instead of hardcoding strings.
"""

PROJECT_NAME = "dshield_prism"
CLI_NAME = "dshield_prism_console"
ENV_PREFIX = "PRISM_"
